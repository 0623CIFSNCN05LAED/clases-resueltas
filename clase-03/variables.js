const firstName = "Juan";
let lastName;
lastName = "Rodriguez";
console.log("name", firstName);

// Tipos de datos

// string
const middleName = "Pedro'123'";
const nickName = `Pepe`; //backtick

// number
const age = 42;

// boolean
const single = false; //true

// array (index)
const pets = ["dog", "cat"];

// object (key: value)
const vehicles = {
  car: {
    qty: 1,
    wheels: 4,
  },
  bikes: 4,
};

// undefined
let deathDate = undefined;

// null
const birthDate = null;

// NaN
const notANumber = NaN;

// function
const myName = function () {
  return "Luios";
};
